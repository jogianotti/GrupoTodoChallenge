{% extends 'base.html.twig' %}

{% block title %}Producto{% endblock %}

{% block body %}
    <h1>Producto</h1>

    <p>{{ 'Home / ' ~ path ~ product.name }}</p>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Descripción</th>
        </tr>
        <tr>
            <td>{{ product.id }}</td>
            <td>{{ product.name }}</td>
            <td>{{ product.description }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('index') }}">Volver al listado</a>
    {% if is_granted('ROLE_USER') %}
        <a href="{{ path('producto_edit', {'id': product.id}) }}">Editar</a>

        {{ include('producto/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
