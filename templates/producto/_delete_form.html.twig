<form method="post" action="{{ path('producto_delete', {'id': product.id}) }}"
      onsubmit="return confirm('Está seguro de eliminar el producto??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn">Eliminar</button>
</form>
