{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    {{ include('categoria/_categories_menu.html.twig') }}

    <h4>Productos</h4>
    <ul>
        {% for product in products %}
            <li>
                Nombre: {{ product.name }}<br>
                <a href="{{ path('producto_show', {'id': product.id}) }}">VER</a>
            </li> <br>
        {% endfor %}
    </ul>
{% endblock %}
